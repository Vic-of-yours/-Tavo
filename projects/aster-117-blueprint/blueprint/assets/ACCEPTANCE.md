# 素材放行门（r002）

- [x] `assets/catalog.json` 恰好覆盖 94 个视觉文件，新路径、旧路径、短存储名、assetId 和语义键各自唯一。
- [x] 94 个视觉文件 SHA-256 各不相同；代码包与素材包成员哈希交集为 0。
- [x] 78 个 SVG 均可解析，内部 `href="#id"` 与 `url(#id)` 均能解析到本文件 ID。
- [x] 16 个栅格均可解码，格式与尺寸和 catalog 一致，人物/立绘/头像分类为 0。
- [x] 95 个运行资源（94 视觉 + catalog）在三处注册表中拥有唯一语义身份与短 storedName。
- [x] 旧物理文件名在运行模块和浏览器素材注入中为 0 命中。
- [x] 60 个通用图标、5 个 HUD 图标、6 个遮罩及 3 个 SVG 合集职责分离。
- [ ] Playwright 真实浏览器完成 394px/宽屏布局与素材 ready 状态验证。
- [ ] Tavo 真机完成 95 项安装、权限、逐项 readback 与缺失降级验证。

后两项在执行前必须保持 `PENDING`，不能用 Node mock 或静态校验代替。
